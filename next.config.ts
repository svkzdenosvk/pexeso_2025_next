// Import the type definition for Next.js configuration (optional but useful for TypeScript support)

import type { NextConfig } from 'next';

/**
 * This file defines the configuration for the Next.js application.
 *
 * You can customize the following settings here:
 * - internationalization (i18n)
 * - image domains and optimization
 * - environment variables
 * - custom webpack settings
 * - redirects, rewrites, headers, etc.
 *
 * Currently, this configuration is empty, using all default settings.
 */

const nextConfig: NextConfig = {
  /* Add custom config options here as needed */
};

export default nextConfig;
